const ParentConfiguration = require("./ParentConfiguration");

module.exports = {
  AddToCard: ParentConfiguration.Parent + "",
  GetAllCardDetails: ParentConfiguration.Parent + "",
  RemoveCartProduct: ParentConfiguration.Parent + "",
  OrderProduct: ParentConfiguration.Parent + "",
};
